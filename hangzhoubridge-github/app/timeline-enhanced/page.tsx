'use client'

import { DataStoryPage } from '@/components/timeline'

/**
 * 数据故事页面 - 增强版
 *
 * 使用完整版 DataStoryPage 组件，包含：
 * - 丰富的叙事性文字
 * - 数据洞察和统计亮点
 * - 强联动机制（时间线、故事、图表、词云）
 * - 自动旅程功能
 * - 光晕和高亮效果
 *
 * 访问: /timeline-enhanced
 */
export default function TimelineEnhancedPage() {
  return <DataStoryPage />
}
